[Unit]
Description=Boxcutter VM network (per-TAP fwmark routing)
Before=caddy.service

[Service]
Type=oneshot
ExecStart=/usr/local/bin/boxcutter-net up
ExecStop=/usr/local/bin/boxcutter-net down
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
