#!/bin/bash
set -e

# Stop the service if running (but don't stop VMs — they keep running)
if systemctl is-active --quiet boxcutter-host.service 2>/dev/null; then
    systemctl stop boxcutter-host.service
fi
